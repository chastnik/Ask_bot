# Зависимости, удаленные из requirements.txt как неиспользуемые
# Дата удаления: 2025-01-23

# HTTP клиенты (используется только aiohttp)
requests>=2.32.0                    # ❌ Не используется в коде
httpx>=0.27.0                       # ❌ Не используется в коде

# База данных
alembic>=1.13.2                     # ❌ Миграции БД не настроены

# Графики (используется только plotly)
matplotlib>=3.9.0                   # ❌ Не используется, только plotly
seaborn>=0.13.2                     # ❌ Не используется, только plotly

# NLP и AI (RAG система не реализована)
sentence-transformers>=3.0.0        # ❌ Настройка есть в config, но не используется
chromadb>=0.5.0                     # ❌ RAG система пока не реализована  
numpy>=1.26.0                       # ❌ Не используется напрямую

# Аутентификация и JWT
python-jose[cryptography]>=3.3.0    # ❌ JWT не используется
passlib[bcrypt]>=1.7.4              # ❌ Используется только cryptography.Fernet

# Тестирование (тестов нет в проекте)
pytest>=8.0.0                       # ❌ Тесты не написаны
pytest-asyncio>=0.23.0              # ❌ Тесты не написаны

# Примечания:
# - sentence-transformers и chromadb могут понадобиться в будущем для RAG системы
# - alembic понадобится, если потребуются миграции БД
# - pytest пакеты следует добавить, когда будут написаны тесты
# - requests/httpx не нужны, так как используется aiohttp везде 